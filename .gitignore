# dist is committed on purpose: the app is deployed as prebuilt artifacts so the
# shared-hosting server only installs runtime dependencies and never builds.
node_modules
dist-ssr
*.local
.env
.env.*
!.env.example
*.log
logs
coverage
uploads/
victoria-braids/
.DS_Store
.trae
models.md
database_schema.sql
prisma/dump-data.ts
pricing_data.xlsx

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
